/**
 * @file
 * nmhockey behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.nmhockey = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
